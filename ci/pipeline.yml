resources:
- name: flight-school
  type: git
  source:
    uri: https://github.com/EmilyBie/flight-school
    branch: master

jobs:
- name: test-app
  plan:
  - get: flight-school
    trigger: true
    version: every
  - task: tests
    file: flight-school/build.yml
